export type GameMetadata = {
    id: string;
    name: string;
    description: string;

    created_at: string;
    updated_at: string;
};

export type GameDetails = {
    id: string;
    name: string;
    description: string;

    source_code: string;

    created_at: string;
    updated_at: string;
};
