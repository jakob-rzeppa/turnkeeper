// Weak entities are entities that do not have an id, and only exist as part of a parent entity.
pub mod weak {
    pub mod action;
    pub mod log;
    pub mod page;
    pub mod player;
    pub mod stat;
}
pub mod game;
pub mod game_instance;
